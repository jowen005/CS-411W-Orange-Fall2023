from rest_framework import serializers
from rest_framework.authtoken.models import Token

from .models import Restaurant

# Responsible for validating and serializing the data uploaded by restaurant users and patron users when interacting with their menus

